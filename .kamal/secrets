# Secrets are pulled from local files — never stored raw here.
# .kamal/registry_token and .kamal/db_password are gitignored.

KAMAL_REGISTRY_PASSWORD=$(cat .kamal/registry_token)
RAILS_MASTER_KEY=$(cat config/master.key)
POSTGRES_PASSWORD=$(cat .kamal/db_password)
DATABASE_URL="postgresql://epm_registry:$(cat .kamal/db_password)@epm_registry-db/epm_registry_production"
